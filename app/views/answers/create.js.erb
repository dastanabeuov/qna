<% if  @answer.errors.present? %>
  $('.answer-errors').append('<%= render 'shared/errors', resource: @answer %>');
<% else %>
  $('.answer-errors').html('')
  $('.answers').html('<%= j render @question.answers %>')
<% end %>
