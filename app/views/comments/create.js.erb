var parentSelector = '<%= @commentable.is_a?(Question) ? ".question-#{@commentable.id}" : ".answer-#{@commentable.id}" %>';
$(parentSelector + ' .comments').html('<%= j render "list", resource: @commentable %>');
